/**
 * @file memory_pool.cc
 * @brief MiniRTC memory pool implementation source
 */

#include "minirtc/memory_pool.h"

// Note: Most of the implementation is in the header file (templates and inline functions).
// This file can be used for additional utilities or explicit template instantiations if needed.

// Explicit template instantiations (if required for non-header-only usage)
// template class minirtc::ObjectPool<SomeType>;

